#include <stdio.h>

int main()
{
    int x,y;
    printf("Enter X value:");
    scanf("%d",&x);
    printf("Enter Y value:");
    scanf("%d",&y);
    if (!(x ^ y))
        printf(" %d is equal to %d ", x, y);
    else
        printf(" %d is not equal to %d ", x, y);

    return 0;
}
